# GhostVault - Secure Vault System

A student-grade secure vault system with real/decoy vault support, panic wipe functionality, and brute-force protection using modern cryptography.

## 🚀 Quick Start

### One-Click Run on Replit
1. Click the "Run" button in Replit
2. Navigate to the web interface at the provided URL
3. Create your first vault system or unlock existing vaults

### Local Installation
```bash
# Clone or download the project
cd ghostvault

# Install dependencies
pip install flask cryptography argon2-cffi pytest

# Run the application
python main.py
